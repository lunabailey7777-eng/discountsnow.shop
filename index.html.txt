<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Reward Offer</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --text: #111111;
      --muted: #737373;
      --light: #f6f6f6;
      --border: #e8e8e8;
      --accent: #ff2d55;
      --accent-dark: #e51d45;
      --green: #20b26b;
    }

    body {
      min-height: 100vh;
      background: #f7f7f7;
      color: var(--text);
      font-family: "Inter", sans-serif;
      display: flex;
      justify-content: center;
      padding: 18px 16px 35px;
    }

    .page {
      width: 100%;
      max-width: 470px;
    }

    /* TOP */

    .top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 18px;
      padding: 0 3px;
    }

    .brand {
      font-size: 16px;
      font-weight: 800;
      letter-spacing: -0.5px;
    }

    .online {
      display: flex;
      align-items: center;
      gap: 6px;
      color: #707070;
      font-size: 11px;
      font-weight: 600;
    }

    .online-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--green);
      box-shadow: 0 0 0 3px rgba(32,178,107,.10);
    }

    /* MAIN */

    .main-card {
      background: #fff;
      border: 1px solid #ededed;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 12px 45px rgba(0,0,0,.07);
    }

    .hero {
      padding: 42px 28px 34px;
      text-align: center;
    }

    .pill {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      background: #f5f5f5;
      border: 1px solid #ebebeb;
      border-radius: 999px;
      padding: 8px 13px;
      font-size: 11px;
      font-weight: 700;
      color: #555;
      margin-bottom: 22px;
    }

    .pill-icon {
      font-size: 13px;
    }

    h1 {
      font-size: clamp(31px, 8vw, 42px);
      line-height: 1.04;
      letter-spacing: -1.8px;
      font-weight: 800;
      margin-bottom: 14px;
    }

    .hero-description {
      max-width: 360px;
      margin: 0 auto;
      color: var(--muted);
      font-size: 14px;
      line-height: 1.55;
    }

    /* REWARD */

    .reward {
      margin: 4px 20px 0;
      padding: 26px 20px 24px;
      background: #fafafa;
      border: 1px solid #ededed;
      border-radius: 20px;
      text-align: center;
    }

    .reward-label {
      color: #8a8a8a;
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 7px;
    }

    .reward-value {
      font-size: 52px;
      line-height: 1;
      font-weight: 800;
      letter-spacing: -2.8px;
    }

    .reward-type {
      color: #777;
      font-size: 12px;
      margin-top: 9px;
    }

    /* CONTENT */

    .content {
      padding: 26px 24px 28px;
    }

    .section-title {
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: .8px;
      color: #999;
      margin-bottom: 14px;
    }

    .steps {
      display: flex;
      flex-direction: column;
      gap: 12px;
      margin-bottom: 24px;
    }

    .step {
      display: flex;
      align-items: center;
      gap: 13px;
    }

    .step-number {
      width: 32px;
      height: 32px;
      flex-shrink: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #f1f1f1;
      border-radius: 10px;
      font-size: 12px;
      font-weight: 800;
    }

    .step-text {
      font-size: 13px;
      line-height: 1.4;
      color: #404040;
    }

    /* CTA */

    .cta {
      display: block;
      width: 100%;
      border: 0;
      border-radius: 14px;
      padding: 17px 18px;
      background: var(--accent);
      color: white;
      text-align: center;
      text-decoration: none;
      font-size: 15px;
      font-weight: 800;
      letter-spacing: -.1px;
      box-shadow: 0 8px 20px rgba(255,45,85,.20);
      transition:
        transform .15s ease,
        background .15s ease,
        box-shadow .15s ease;
    }

    .cta:hover {
      background: var(--accent-dark);
      transform: translateY(-1px);
      box-shadow: 0 11px 25px rgba(255,45,85,.25);
    }

    .cta:active {
      transform: translateY(0);
    }

    .cta-arrow {
      margin-left: 5px;
    }

    .secure {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 6px;
      margin-top: 13px;
      color: #999;
      font-size: 10px;
      font-weight: 500;
    }

    /* FOOTER */

    .footer {
      text-align: center;
      margin-top: 17px;
      padding: 0 12px;
      color: #a0a0a0;
      font-size: 9px;
      line-height: 1.6;
    }

    /* MOBILE */

    @media (max-width: 380px) {
      body {
        padding: 12px 11px 25px;
      }

      .hero {
        padding: 34px 20px 28px;
      }

      .content {
        padding: 23px 19px 24px;
      }

      .reward {
        margin-left: 15px;
        margin-right: 15px;
      }

      h1 {
        font-size: 30px;
      }

      .reward-value {
        font-size: 46px;
      }
    }
  </style>
</head>

<body>

  <main class="page">

    <header class="top">
      <div class="brand">DiscountsNow</div>

      <div class="online">
        <span class="online-dot"></span>
        Online
      </div>
    </header>

    <section class="main-card">

      <div class="hero">

        <div class="pill">
          <span class="pill-icon">🎁</span>
          Reward offer
        </div>

        <h1>
          Get rewarded<br>
          for your opinion.
        </h1>

        <p class="hero-description">
          Complete a short survey and check your eligibility
          for the reward shown below.
        </p>

      </div>

      <div class="reward">

        <div class="reward-label">
          Available reward
        </div>

        <div class="reward-value">
          $750
        </div>

        <div class="reward-type">
          Gift card reward
        </div>

      </div>

      <div class="content">

        <div class="section-title">
          How it works
        </div>

        <div class="steps">

          <div class="step">
            <div class="step-number">1</div>
            <div class="step-text">
              Start the offer by clicking the button below.
            </div>
          </div>

          <div class="step">
            <div class="step-number">2</div>
            <div class="step-text">
              Complete the short survey and any required steps.
            </div>
          </div>

          <div class="step">
            <div class="step-number">3</div>
            <div class="step-text">
              Follow the instructions provided to receive your reward.
            </div>
          </div>

        </div>

        <a
          class="cta"
          href="href="https://giftclick.org/aff_c?offer_id=4676&aff_id=154633"
        >
          Check My Eligibility
          <span class="cta-arrow">→</span>
        </a>

        <div class="secure">
          🔒 Secure connection
          <span>•</span>
          🔐 Your information is protected
        </div>

      </div>

    </section>

    <footer class="footer">
      Promotional offer. Eligibility and reward availability may vary.
      Survey participation and additional requirements may apply.
    </footer>

  </main>

</body>
</html>